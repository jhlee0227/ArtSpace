<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hall">

	<insert id="insert">
		insert into hall(hall_name, address1, address2, hall_description, warning, create_date, area, width, length, height, maximum)
		values(#{hall_name}, #{address1}, #{address2}, #{hall_description}, #{warning}, #{create_date}, #{area}, #{width}, #{length}, #{height}, #{maximum})
	</insert>

	<select id="findById" resultType="com.example.demo.hall.dto.HallDTO">
		select * from hall
		where hall_id = #{id}
	</select>

</mapper>